# wireshark-wamp-plugin
Wireshark plugin that decodes the WAMP protocol (http://wamp-proto.org/)

# Installation

Copy files to `~/.wireshark/plugins` (or where your system configuration stores Wireshark plugins)
